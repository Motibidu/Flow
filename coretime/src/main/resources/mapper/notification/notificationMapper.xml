<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flow.coretime.notification.mapper.NotificationMapper">

        <insert id="insertNotification" 
                parameterType="com.flow.coretime.notification.NotificationDTO" 
                useGeneratedKeys="true" 
                keyProperty="notifId">
        INSERT INTO notifications (
                recipient_id, title, message, target_url, is_read, created_at
        ) VALUES (
                #{recipientId}, #{title}, #{message}, #{targetUrl}, #{isRead}, #{createdAt}
        )
        </insert>

        <select id="selectRecentNotifications" resultType="com.flow.coretime.notification.NotificationDTO">
                SELECT * FROM notifications 
                WHERE recipient_id = #{userId}
                ORDER BY 
                        (CASE WHEN is_read = 'UNREAD' THEN 0 ELSE 1 END) ASC,
                        created_at DESC
                LIMIT 10
        </select>


        <update id="markAsRead">
                UPDATE notifications 
                SET is_read = 'READ' 
                WHERE notif_id = #{notifId}
        </update>

        <update id="markAllAsRead">
                UPDATE notifications
                SET is_read= 'READ'
                WHERE recipient_id = #{userId}
        </update>

        <select id="selectAllNotifications" resultType="com.flow.coretime.notification.NotificationDTO">
                SELECT * FROM notifications 
                WHERE recipient_id = #{userId}
                ORDER BY 
                        (CASE WHEN is_read = 'UNREAD' THEN 0 ELSE 1 END) ASC,
                        created_at DESC
        </select>




</mapper>
